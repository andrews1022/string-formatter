import React from 'react';

interface FormatterProps {}

const Formatter = (props: FormatterProps) => {
	return (
		<div>
			<h2>Hello from the Formatter component!</h2>
		</div>
	);
};

export default Formatter;
